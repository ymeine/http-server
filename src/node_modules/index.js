require('LiveScript');

var zappa = require('zappajs');

var Server = require('./server').server.Server;


function run(routes, options, logger) {
	return Server({
		system: zappa,
		routes: routes,
		options: options
	}, logger).run();
};



exports.rpc = require('rpc');
exports.server = require('server');

exports.run = run;
